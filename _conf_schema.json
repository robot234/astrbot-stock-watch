{
  "enabled": {"type": "bool", "description": "启用后台监听和收盘扫描", "default": true},
  "push_whitelist": {"type": "string", "description": "后台推送白名单，填写 unified_msg_origin，多个值用逗号或换行分隔；留空时仅允许通过/白名单 开启加入的会话；填写 * 将允许所有会话", "default": ""},
  "allow_self_whitelist": {"type": "bool", "description": "允许会话通过指令自行加入白名单，默认关闭", "default": false},
  "push_max_chars": {"type": "int", "description": "单次推送最大字符数，超出时按行拆分发送", "default": 3500},
  "daily_cache_enabled": {"type": "bool", "description": "是否每天收盘抓取全市场日快照并缓存到本地", "default": true},
  "daily_cache_keep_days": {"type": "int", "description": "全市场日快照保留天数", "default": 180},
  "daily_snapshot_min_size": {"type": "int", "description": "判定完整全市场收盘快照的最少有效股票数", "default": 4000},
  "calendar_ttl_seconds": {"type": "int", "description": "已确认交易日状态的缓存 TTL 秒数", "default": 86400},
  "calendar_unknown_ttl_seconds": {"type": "int", "description": "未知交易日状态的短 TTL 秒数，过期后允许重试", "default": 900},
  "daily_market_url": {"type": "string", "description": "每日全市场快照接口地址", "default": ""},
  "factor_mode": {"type": "string", "description": "因子模式：report_only 只展示，score 才参与排序", "default": "report_only"},
  "factor_data_url": {"type": "string", "description": "行业/基本面兼容 JSON 接口地址，留空使用可用内置来源", "default": ""},
  "factor_source": {"type": "string", "description": "因子来源：auto/eastmoney/tushare/custom", "default": "auto"},
  "market_min_snapshot_size": {"type": "int", "description": "判定完整市场环境的最少快照数量", "default": 4000},
  "tushare_url": {"type": "string", "description": "Tushare Pro 接口地址", "default": ""},
  "tushare_token": {"type": "string", "description": "Tushare Pro Token", "default": "", "obvious_hint": true},
  "universe_codes": {"type": "string", "description": "选股代码，逗号分隔；留空时使用自选股", "default": ""},
  "watchlist_limit": {"type": "int", "description": "每个会话最多保存的自选股数量", "default": 100},
  "quote_interval_seconds": {"type": "int", "description": "盘中自选股行情轮询间隔", "default": 30},
  "minute_enabled": {"type": "bool", "description": "是否记录盘中一分钟聚合行情", "default": true},
  "minute_bar_history": {"type": "int", "description": "每只股票保留的已完成分钟线数量", "default": 120},
  "minute_bar_keep_days": {"type": "int", "description": "SQLite 保留已完成分钟线的天数", "default": 7},
  "minute_trigger_enabled": {"type": "bool", "description": "是否启用分钟线突破提醒；默认关闭", "default": false},
  "minute_trigger_lookback": {"type": "int", "description": "分钟突破参考历史根数，仅机械条件，不代表趋势延续", "default": 5},
  "minute_trigger_min_bars": {"type": "int", "description": "分钟触发所需的最少已完成根数", "default": 5},
  "minute_trigger_consecutive_up": {"type": "int", "description": "分钟触发要求连续上涨根数", "default": 3},
  "minute_trigger_step_pct": {"type": "float", "description": "每根分钟线最小涨幅百分比，仅为触发阈值", "default": 0.1},
  "minute_trigger_breakout_pct": {"type": "float", "description": "突破历史高点所需的百分比，仅为触发阈值", "default": 0.5},
  "intraday_failure_threshold": {"type": "int", "description": "连续行情失败达到该次数后暂缓信号推送；0表示只统计不暂缓", "default": 0},
  "confirmation_enabled": {"type": "bool", "description": "是否启用连续信号确认", "default": false},
  "confirmation_periods": {"type": "int", "description": "连续满足条件的次数", "default": 2},
  "confirmation_max_gap_seconds": {"type": "int", "description": "连续确认最大间隔秒数", "default": 90},
  "cost_profit_threshold_pct": {"type": "float", "description": "(现价-成本)/成本达到该百分比的收益观察阈值，未扣费滑点，仅用于复核", "default": 5.0},
  "cost_risk_threshold_pct": {"type": "float", "description": "(现价-成本)/成本达到该百分比的亏损观察阈值，仅用于复核，不是交易指令", "default": 5.0},
  "news_interval_seconds": {"type": "int", "description": "新闻/RSS轮询间隔", "default": 180},
  "daily_scan_time": {"type": "string", "description": "收盘扫描时间（北京时间）", "default": "15:10"},
  "candidate_limit": {"type": "int", "description": "收盘推送候选数量", "default": 30},
  "report_candidate_limit": {"type": "int", "description": "全市场报告默认展示的候选数量", "default": 10},
  "auto_watch_candidates": {"type": "bool", "description": "盘中自动监听最近候选池", "default": true},
  "candidate_plan_valid_days": {"type": "int", "description": "收盘候选价位计划用于盘中监听的有效天数", "default": 10},
  "price_plan_close_tolerance_pct": {"type": "float", "description": "收盘计划参考价与未复权日线最后收盘价允许的最大偏差百分比", "default": 1.0},
  "intraday_focus_limit": {"type": "int", "description": "盘中重点监听数量上限", "default": 200},
  "deep_screen_limit": {"type": "int", "description": "技术深筛最多处理的高流动性股票数量", "default": 300},
  "factor_screen_limit": {"type": "int", "description": "因子补充和终评最多处理的深筛股票数量", "default": 100},
  "screen_min_indicator_coverage": {"type": "float", "description": "完整空结果允许清理候选池所需的技术指标覆盖率", "default": 0.8},
  "min_score": {"type": "int", "description": "技术评分最低分（建议先用10，分数只用于排序）", "default": 10},
  "fallback_limit": {"type": "int", "description": "无达标项时最多保留的观察候选数", "default": 5},
  "price_min": {"type": "float", "description": "最低股价", "default": 2},
  "price_max": {"type": "float", "description": "最高股价", "default": 80},
  "news_rss_url": {"type": "string", "description": "可选 RSS 地址，留空关闭故事监听", "default": ""},
  "llm_enabled": {"type": "bool", "description": "是否调用模型 API 摘要新闻", "default": false},
  "llm_annotation_enabled": {"type": "bool", "description": "是否调用模型 API 解释盘中候选", "default": false},
  "llm_annotation_interval_seconds": {"type": "int", "description": "盘中模型解释间隔秒数", "default": 180},
  "llm_annotation_limit": {"type": "int", "description": "每批模型解释候选数量", "default": 10},
  "llm_annotation_max_tokens": {"type": "int", "description": "每批模型解释最大 Token 数", "default": 800},
  "llm_min_interval_seconds": {"type": "int", "description": "模型请求最小间隔秒数", "default": 10},
  "llm_daily_request_limit": {"type": "int", "description": "模型每日最大请求次数", "default": 100},
  "llm_base_url": {"type": "string", "description": "OpenAI 兼容接口地址", "default": "https://api.openai.com/v1"},
  "llm_api_key": {"type": "string", "description": "模型 API Key", "default": "", "obvious_hint": true},
  "llm_model": {"type": "string", "description": "模型名称", "default": "gpt-4o-mini"},
  "request_timeout": {"type": "int", "description": "HTTP 请求超时秒数", "default": 10},
  "max_concurrency": {"type": "int", "description": "历史行情和网络任务最大并发数", "default": 5},
  "paper_trading_only": {"type": "bool", "description": "仅研究/模拟盘", "default": true}
}
